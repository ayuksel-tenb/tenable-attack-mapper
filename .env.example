# Copy to .env and fill in. Never commit the real .env (see .gitignore).
# For a quick local test, copy .env_test instead and add your Anthropic key.

# --- Tenable Security Center ---
TSC_URL=https://securitycenter.local
TSC_ACCESS_KEY=your-access-key
TSC_SECRET_KEY=your-secret-key
TSC_VERIFY_SSL=false

# --- Semantic mapping backend ---
# "claude" = use the local `claude` CLI (billed to your Claude Code subscription;
#            no API key, no per-token cost). "api" = Anthropic API (pay per token).
TASC_SEMANTIC_BACKEND=claude
TASC_CLAUDE_MODEL=claude-haiku-4-5

# Only for the "api" backend:
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-haiku-4-5

# --- Mapping behaviour (optional) ---
# TASC_CONFIDENCE_THRESHOLD=0.5      # below this -> flagged needs-review
# TASC_SEMANTIC_WORKERS=8            # concurrent semantic API calls
# TASC_SEMANTIC_NO_CVE=false         # also map no-CVE compliance findings
# TASC_USE_NVD=false                 # live NVD CVE->CWE lookup for misses
# NVD_API_KEY=                       # higher NVD rate limits
