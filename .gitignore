# Secrets — .env is local; .env_test is a committed test template (no real keys)
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Generated outputs
layer.json
*.layer.json
coverage-report.md

# OS / editor
.DS_Store
.idea/
.vscode/

# NVD CVE->CWE cache (warmed at runtime)
data/.nvd_cache.json

# Local artifacts from live runs (exposure data — never commit)
sc-live-layer.json
data/.semantic_cache.json

# Generated coverage report and nested viewer clone (from the show-matrix skill)
coverage.md
/attack-navigator/
